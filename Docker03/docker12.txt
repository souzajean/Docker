# Login docker hub
docker login -u <username> -p <password>

# Tag image previsão build 
docker tag my_image souzajean/myimage:latest

# Push image
docker push souzajean/myimage:latest

# Pull image
docker pull souzajean/myimage:latest